name: Build workflow-testing

on:
  push:
    branches:
      - "**"
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  get-archi:
    strategy:
      matrix:
        python-version: ["3.9", "3.11"]
        os: ["macos-latest", "windows-latest", "ubuntu-latest"]
        backend: [ "tf", "torch", "jax" ]
        autolirpa : [ "", "autolirpa" ]
        exclude:
          - backend: "tf"
            autolirpa: "autolirpa"
          - backend: "jax"
            autolirpa: "autolirpa"
      fail-fast: false
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    steps:

      - name: Pick proper tox env
        shell: python
        run: | 
          toxenv="${{ matrix.python-version }}-${{ matrix.os }}-${{ matrix.backend }}"
          if "${{ matrix.autolirpa }}" == "autolirpa":
              toxenv += "-autolirpa"
          print(toxenv)
      - run: |
          if [[ "${{ matrix.autolirpa }}" != "autolirpa"  ]]; then
            echo python -m tox -e convert-doc-to-test  # extract code blocks from doc to test them
          fi
          

          
